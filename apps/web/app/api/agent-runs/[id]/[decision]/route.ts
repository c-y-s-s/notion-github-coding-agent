import { adminDb } from "@/lib/supabase"; import { failure, ok } from "@/lib/http";
export async function POST(_: Request, { params }: { params: Promise<{id:string;decision:string}> }) { const { id, decision } = await params; if (!['approve','reject'].includes(decision)) return failure("不支援的審核決定"); const db = adminDb(); const { data: run } = await db.from("agent_runs").select("id,work_item_id,status").eq("id", id).maybeSingle(); if (!run) return failure("找不到執行紀錄", 404); if (run.status !== "awaiting_approval") return failure("此執行紀錄目前不在等待核准狀態", 409); const status = decision === "approve" ? "approved" : "rejected"; await db.from("agent_runs").update({ status, finished_at: decision === "reject" ? new Date().toISOString() : null }).eq("id", id); await db.from("work_items").update({ agent_status: decision === "approve" ? "pushing" : "rejected" }).eq("id", run.work_item_id); return ok({ id, status }); }
