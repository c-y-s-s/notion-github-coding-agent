import { adminDb } from "@/lib/supabase"; import { failure, ok } from "@/lib/http";
export async function GET(request: Request) { const q = new URL(request.url).searchParams.get("q")?.trim() ?? ""; const safe = q.replaceAll("%", "").replaceAll("_", ""); let query = adminDb().from("work_items").select("id,title,notion_page_id,planning_status,updated_at").eq("source","notion").not("notion_page_id","is",null).not("notion_page_url","is",null).is("github_issue_node_id",null).neq("title","Untitled").order("updated_at",{ascending:false}).limit(20); if(safe) query=query.ilike("title",`%${safe}%`); const {data,error}=await query; if(error)return failure(error.message,500);return ok(data); }
