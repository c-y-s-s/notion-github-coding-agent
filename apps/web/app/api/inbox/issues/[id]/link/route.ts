import { linkSchema } from "@/lib/schemas";
import { adminDb } from "@/lib/supabase";
import { failure, ok } from "@/lib/http";
export async function POST(request: Request, { params }: { params: Promise<{id:string}> }) { const parsed = linkSchema.safeParse(await request.json()); if (!parsed.success) return failure(parsed.error.message); const { id } = await params; const db = adminDb(); const { data: notionTask } = await db.from("work_items").select("id,notion_page_id").eq("notion_page_id", parsed.data.notionPageId).eq("source", "notion").maybeSingle(); if (!notionTask) return failure("Notion task not found", 404); const { data: issue } = await db.from("work_items").select("*").eq("id", id).eq("review_status", "pending").maybeSingle(); if (!issue) return failure("Issue was already reviewed", 409); const { error } = await db.from("work_items").update({ github_issue_node_id: issue.github_issue_node_id, github_issue_number: issue.github_issue_number, github_issue_url: issue.github_issue_url, github_issue_state: issue.github_issue_state, review_status: "linked" }).eq("id", notionTask.id); if (error) return failure(error.message, 409); await db.from("work_items").delete().eq("id", id); return ok({ linkedTo: notionTask.id }); }

